-r requirements.txt

# Optional dependencies for --mode local.
yt-dlp>=2024.1.1
faster-whisper>=1.0.0
openai>=1.0.0
google-genai>=1.0.0
# OpenCV 5.0 removed the Haar cascade API (CascadeClassifier, cv2.objdetect) and
# the bundled haarcascade XMLs that local/clipper.py depends on for face tracking.
opencv-python>=4.8.0,<5
# torch is only needed if you want CUDA Whisper. CPU works without it.
# torch>=2.0
